channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  # Python
  - python ==3.12
  - pandas ==2.2.2
  - numpy  ==2.0.0
  # Tools
  # Cannot use a later version here than the wrapper uses,
  # as that somehow leads to broken pipes in the calling.
  - trimmomatic ==0.36
  - pigz ==2.3.4
  - snakemake-wrapper-utils ==0.6.2
