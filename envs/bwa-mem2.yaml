channels:
  - bioconda
  - conda-forge
dependencies:
  # Python
  - python ==3.7.10
  - pandas ==1.3.1
  - numpy ==1.21.2
  # Tools
  - bwa-mem2 ==2.2.1
  # Somehow on Resnick, we got /usr/bin/bash: samtools: command not found
  # So let's add it here...
  - samtools ==1.12
